
read x; { input an integer }

while(0 < x) { don't compute if x <= 0 }

  fact := 1;
  while(0 < x)
    fact := fact * x;
    x := x - 1
  endwhile;

  write fact  { output factorial of x }
endwhile
